# Vercel Setup

## CLI Installation
```bash
npm install -g vercel
vercel login
```

## Project Connection
```bash
vercel
```

## Configuration
`vercel.json` (optional but recommended for advanced settings):
```json
{
  "framework": "vite",
  "buildCommand": "npm run build"
}
```
