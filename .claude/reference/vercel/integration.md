# Vercel PIV Integration

## How PIV Works with Vercel

### Implement Phase
- Suggests `vercel.json` configuration.
- Checks environment variable usage.

### Validate Phase
- Can trigger deployment (via CLI or git push).
- Verifies deployment URL.
