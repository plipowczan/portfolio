# Playwright PIV Integration

## How PIV Works with Playwright

### Implement Phase
- Writes tests before implementation (TDD style) or after.
- Uses `data-testid` attributes in React components for stable selectors.

### Validate Phase
- Runs `npm test` to verify all scenarios pass.
